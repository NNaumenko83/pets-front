import styled from '@emotion/styled'
import ContainerStyled from 'src/components/Container/Container.styled'

const UserPageContainer = styled(ContainerStyled)`
    border: 1px solid red;
    display: flex;
    justify-content: space-between;
`

export default UserPageContainer
